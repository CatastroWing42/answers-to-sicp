#lang sicp

;; Eva is right.
;; When we say "algebraically equivalent", we assume that A/A = 1
;; but that's not right for interval-arithmetic
;; So, reduce the computation of interval will provide a better result