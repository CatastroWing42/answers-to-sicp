#lang sicp
; (define balance 100)
; 
; (set! balance (+ balance 50))
; (display balance)
; (newline)
; (display (begin (set! balance (- balance 20)) balance))
; (newline)
; (gcd 2 3)
; (newline)
; (random 10.0)
; 
; (cons 'a (cons '() '()))
; (number? 3)
; (number? 's)
; (< 2 'b)

; (define ll (list 'a 'b 'c))
; (memq 'c ll)
; 
; (sqrt 2)
; (define (show a b)
;     (display a) (newline) (display b))
; (define x
;     (apply show (list 2 4)))

(quotient 10 7)
(remainder 10 7)